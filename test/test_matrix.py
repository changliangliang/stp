from stp import array, stp
from stp.stp import ReducingMatrix


def test_reducing_matrix():
    # arr = array([[1, 0, 0]]).T
    # a = stp(ReducingMatrix(3), stp(arr, arr))
    print(stp)
    # ReducingMatrix(2)
